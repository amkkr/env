# 必ず遵守すべきルール

- 日本語で回答すること
- force-pushで過去のコミットログを消すようなコミット操作はしないこと
- いかなる作業でもサブエージェントを使うこと、必ず作業担当とレビュワーの最低2人を生成すること

## Github Rules

- PRの説明にClaude Codeへの言及を含めないこと
- レビュワーエージェントはレビュー結果をPRにコメントしないこと
- レビューコメントの返信をしないこと

## Git Rules

- **機微情報に絶対触れないこと**: emv, 環境変数の編集は禁止、触れる必要がある場合はユーザーに許可を取ること、例外はない
  - gitignore, gitkeepのようなファイルも確認なしの編集はしないこと
- **コミットメッセージのルール**: コミットメッセージにCo-Authored-Byを含めないこと
- **コミット粒度のルール**: 対応内容ごとにコミットすること
- **masterへの直接コミット禁止**: masterブランチに直接コミットしてはならない
- **ブランチ作成必須**: ファイルを操作する場合は必ずブランチを切ってから編集・コミットすること
- **ブランチ名のプレフィックス**: ブランチ名には必ず以下のプレフィックスをつけること
  - `feat/` - 新機能開発
  - `fix/` - バグ修正
  - `chore/` - ドキュメント更新や自動生成ファイルの更新など
  - `refactor/` - コードのリファクタリング
  - `update/` - ドキュメントや設定ファイルの更新
- **英語でのブランチ名**: ブランチ名は英語で記述すること
- **簡潔な命名**: ブランチ名は短く、わかりやすいものにする
- **直接マージ禁止**: 開発ブランチを直接masterにマージしてはならない
- **Pull Request必須**: マージは必ずGithubにpushして、Pull Requestを通して行うこと
- **rebase禁止**: git rebaseコマンドは使用しない。コンフリクト解決時はgit mergeを使用すること
- **PRテンプレート使用**: Pull Requestの内容は`.github/pull_request_template.md`を元に作成すること
- **PRタイトルのプレフィックス**: PRタイトルにはブランチ名と同様のプレフィックスをつけること（例: `update: CLAUDE.mdにルールを追加`、`feat: ユーザー認証機能を追加`）

### ブランチ名の例

```bash
# 良い例
feat/user-authentication
fix/login-error
chore/update-dependencies
refactor/improve-performance
update/claude-md

# 悪い例
new-feature
修正
my-branch
```

## 作業規律(プロジェクト共通)

- **設計レビュー(DAレビュー)は独立エージェントで行うこと**: 自分の中の1パスで済ませず、設計担当とDA(devil's advocate)を別エージェントとして立て、複数ラウンド壁打ちすること
- **push前のローカル承認**: レビュワー(およびDA)のローカル承認を得てからpushすること
- **停止指示の遵守**: 「Issue NまででSTOP」等の指示があったら、当該PRのマージ完了まで次のIssueに着手しないこと。並列実装は明示的に指示された場合のみ
- **設定・手順の適用前検証**: ユーザー提供の設定やドキュメントの記述でも、参照先のディレクトリ・ツール・ファイルがリポジトリに実在するか確認してから適用すること
- **用語・方法論は推測しない**: 方法論・用語の解釈が不明なら、推測で機械的な変換をせず、まずユーザーに確認すること(例: t-wada式TDDはRed-Green-Refactorの実践のことで、expect→assertのようなツール置換ではない)
- **コミット前にlintを実行すること**
- **外部API仕様は一次情報で裏取り**: API指標やURLを成果物に書くときは、公式ドキュメントをWebFetchで実際に確認してから記載すること。推測でURLや仕様を書かない

## エージェント/ツールの既知の落とし穴

- **サブエージェントのgit成果検証**: worktreeでコミットしたサブエージェントの成果を親から確認するときは、サンドボックス無効(dangerouslyDisableSandbox: true)のBashで検証すること。サンドボックス有効だと空・古い状態が見えて「未着手」と誤認する
- **worktreeからのgh pr merge**: linked worktreeから`gh pr merge --squash --delete-branch`するとローカル後処理は失敗するが、GitHub側のマージは成功している。`gh pr view --json state`でMERGEDを確認してから後始末すること
- **カスタムagentの実行**: `.claude/agents/*.md`のカスタムagentはAgentツールのsubagent_typeでは指定できない。general-purposeエージェントに定義ファイルをReadさせて実行すること
- **Workflowツールのargs**: `{scriptPath, args}`起動でargsがundefinedになることがある。パラメータはscript冒頭に直書きすること。スキーマ付きエージェントの並行は4〜6に抑える(16並行はリソース枯渇で全滅)
- **@claudeボットのレビュー監視**: Monitorツールはバックグラウンドでgh未認証のため使えない。Bash(run_in_background)のuntilループで「Claude finished」を待ち、push後に新規コメントが付いたかで完了判定すること(開始直後の誤検知に注意)
